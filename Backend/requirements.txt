# Teledent AI — Python dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.2.0
google-auth==2.30.0

# Cloudinary
cloudinary==1.36.0

# LiveKit (video)
livekit-api==0.7.1

# Email
resend==2.30.1

# PDF (WeasyPrint + Jinja2) — pure Python, no browser install required
weasyprint==62.3
pydyf<0.11.0
jinja2==3.1.4

# AI / ML (dental vision + explanation)
numpy>=1.26.0             # directly used in keras_oral_service + keras_xray_service
tensorflow>=2.16.0        # Keras models: VGG16 oral (vgg16_final_best.keras) + CNN X-ray (best_xray_3class.keras)
Pillow==10.4.0
langchain-google-genai==1.0.10
google-genai>=1.0.0

# Image preprocessing (denoising / grain removal)
opencv-python-headless==4.10.0.84

# Payments
stripe==10.5.0

# HTTP client
httpx==0.27.0

# Rate limiting
slowapi==0.1.9

# Env
python-dotenv==1.0.1
python-multipart==0.0.9
